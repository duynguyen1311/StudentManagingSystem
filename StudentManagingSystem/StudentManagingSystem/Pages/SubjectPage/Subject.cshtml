@page
@model StudentManagingSystem.Pages.SubjectPage.SubjectModel
@{
}
<div class="mobile-menu-overlay"></div>

<div class="main-container">
    <div class="pd-ltr-20 xs-pd-20-10">
        <div class="min-height-200px">
            <div class="page-header">
                <div class="row">
                    <div class="col-md-6 col-sm-12">
                        <div class="title">
                            <h4>Department List</h4>
                        </div>
                        <nav aria-label="breadcrumb" role="navigation">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a asp-page="Index">Home</a></li>
                                <li class="breadcrumb-item active" aria-current="page">Subject</li>
                            </ol>
                        </nav>
                    </div>
                    <div class="col-md-6 col-sm-12 text-right">
                        <div class="dropdown">
                            <a class="btn btn-primary" asp-page="AddSubject">
                                Add Subject
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="table-responsive">
                <table class="table table-striped table-bordered ">
                    <thead style="background-color: lightgray">
                        <tr>
                            <th>Subject Name</th>
                            <th>Subject Code</th>
                            <th>Status</th>
                            <th>Description</th>
                            <th>Semester</th>
                            <th>Created Date</th>
                            <th>Last Modified Date</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.ListSubject)
                        {
                            <tr>
                                <td class="table-plus">@item.SubjectName</td>
                                <td>@item.SubjectCode</td>
                                <td>@(item.Status == true ? "Hoạt động" : "Không hoạt động")</td>
                                <td>@item.Description</td>
                                <td>@item.Semester</td>
                                <td>@(item.CreatedDate != null ? item.CreatedDate.Value.ToShortDateString() : "No Data")</td>
                                <td>@(item.LastModifiedDate != null ? item.LastModifiedDate.Value.ToShortDateString() : "No Data")</td>
                                <td>
                                    <div class="dropdown">
                                        <a class="btn btn-link font-24 p-0 line-height-1 no-arrow dropdown-toggle" href="#" role="button" data-toggle="dropdown">
                                            <i class="dw dw-more"></i>
                                        </a>
                                        <div class="dropdown-menu dropdown-menu-right dropdown-menu-icon-list">
                                            <a class="dropdown-item" asp-page="UpdateDepartment" asp-route-id="@item.Id"><i class="dw dw-edit2"></i> Edit</a>
                                            <a class="dropdown-item" asp-page="DeleteDepartment" asp-route-id="@item.Id" onclick="return confirm('Are you sure you want to delete ?')"><i class="dw dw-delete-3"></i> Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>